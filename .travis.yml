language: java

sudo: false

matrix:
  include:
    - os: linux
      dist: precise
      jdk:
        - oraclejdk7
        - openjdk7
    - os: linux
      dist: trusty
      jdk:
        - oraclejdk8
        - oraclejdk9

after_success:
  - mvn clean cobertura:cobertura coveralls:report
